{
  "name": "purescript-hatter",
  "version": "0.0.1",
  "description": "A template engine for Purescript and virtual-dom",
  "devDependencies": {
    "virtual-dom": "^2.1.1",
    "eslint": "^3.19.0",
    "pulp": "^11.0.0",
    "purescript-psa": "^0.5.1",
    "rimraf": "^2.6.1"
  },
  "bin": { "hatter": "index.js" },
  "main": "hatter.js",
  "files": [
    "index.js",
    "hatter.js"
  ],
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "eslint src && pulp build -- --censor-lib --strict && psc-bundle \"output/*/*.js\" --output hatter.js --module Main --main Main",
    "test": "rimraf \"test/Fixture/*.purs\" && ./index.js test/Fixture && pulp test"
  }
}
