{
  "name": "purescript-hatter",
  "version": "0.0.1",
  "description": "A template engine for Purescript and virtual-dom",
  "dependencies": {},
  "devDependencies": {
    "virtual-dom": "^2.1.1",
    "jscs": "^2.9.0",
    "jshint": "^2.9.1",
    "pulp": "^9.0.1",
    "rimraf": "^2.5.1"
  },
  "scripts": {
    "postinstall": "bower install",
    "dist": "pulp browserify --to index.js",
    "build": "jshint src && jscs src && pulp build && rimraf docs && pulp docs"
  }
}
